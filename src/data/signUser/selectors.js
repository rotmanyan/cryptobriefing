export const isAuthenticated = state => state.session.isAuthenticated
export const isResetPassword = state => state.session.isResetPassword
export const isMessage = state => state.session.message
export const tokenUser = state => state.session.token